# Sync all modules and submodules with remotes

## Audit results (verified 2026-08-26, read-only)

| Module | Recorded pointer | Local checkout | Remote HEAD | Status |
|---|---|---|---|---|
| Root (morteza-taheri/SoftEther) | — | `e5d9985` | `origin/master` = `e5d9985` | ✅ pushed, clean |
| `sstpClient` | `2af036f` | `2af036f` | `vpngate-connector` = `2af036f` | ✅ up to date |
| `vpnLib` | `f63b4b6` | `f63b4b6` | `main` = `f63b4b6` | ✅ up to date |
| `SoftEtherClient` | `0fca2e1` | `0fca2e1` (detached HEAD) | `main` = `ba6c645` | ⚠️ `0fca2e1` "Update OpenSSL 3.5.7 to 3.5.8 LTS" exists on NO remote ref — root pointer is unreachable for fresh clones and CI (`submodules: 'recursive'` checkout will fail) |
| `SoftEtherClient` → `src/main/cpp/openssl` | `f4dc4d5` | `f4dc4d5` = tag `openssl-3.5.8` | latest 3.5.x tag is `openssl-3.5.8` | ✅ pinned, up to date |
| `SoftEtherClient` → `SoftEtherVPN_Stable` | `ed17437` | `ed17437` = `v4.44-9807-rtm` | latest release tag is `v4.44-9807-rtm` | ✅ pinned, up to date |
| `openssl` → `pkcs11-provider` | `64fc325` | `663dea3` (drift) | — | ℹ️ checkout drift only; unused by Android build |

Environment notes:
- Remotes are HTTPS (`https://github.com/hoang-rio/...`), writable main already received earlier commits of this series (`ba6c645`, `f80c1fc`...), so a push is expected to work.
- `SoftEtherClient` local branch `main` is stale at `c889c28` (ancestor of `0fca2e1`, 31 commits behind); checkout is detached at `0fca2e1`.
- Root worktree " M SoftEtherClient" = modified content propagating from `openssl` → `pkcs11-provider` checkout drift (build artifacts). No pointer changes. Untracked `.kilo/plans/` stays uncommitted.

## Decision (user-confirmed)
Publish `0fca2e1` by pushing directly to the existing `main` of `hoang-rio/SoftEther-Android-Module`. No `.gitmodules`/CI changes.

## Tasks
1. In `SoftEtherClient/` workdir: fast-forward local branch to the detached HEAD, then push:
   - `git branch -f main HEAD` (align local `main` with `0fca2e1`)
   - `git checkout main` (leave submodule on a branch instead of detached HEAD)
   - `git push origin main`
2. Optional cleanup (harmless): re-sync the drifted nested checkout without touching pointers:
   - In `SoftEtherClient/src/main/cpp/openssl/`: `git submodule update pkcs11-provider`
   - If this makes `git status` in `SoftEtherClient` report anything under `src/main/cpp/openssl`, leave it — do NOT commit anything inside openssl.
3. No changes needed in `sstpClient`, `vpnLib`, or the root repo (all verified in sync; nothing to commit).

## Validation
1. `git ls-remote https://github.com/hoang-rio/SoftEther-Android-Module.git main` → returns `0fca2e102c7b1304f950203a7d353dda0f7c42e8`.
2. Root `git status --porcelain` → only `m SoftEtherClient` (modified content) at most, no ` M` pointer change; `git log --oneline -1` still `e5d9985`.
3. Fresh-clone simulation (optional): `git clone --recurse-submodules` of `https://github.com/morteza-taheri/SoftEther.git` into a temp dir resolves all submodule pointers.
4. CI health check: next `master` push CI run should pass the submodule checkout step (verify on GitHub Actions after any subsequent push).

## Risks / failure modes
- Push rejected (no write access to `hoang-rio/SoftEther-Android-Module`): fall back to asking the user to grant access or to create a fork + update `.gitmodules` URLs (root) — stop and re-plan if this happens.
- Do not run `git submodule update --init --recursive` from the root before pushing — it would try to fetch the unreachable `0fca2e1` and fail confusingly.
- Bash permission mode is restrictive: use allowed read-only git commands for validation; submodule push needs an implementation-capable agent.

## Out of scope
- Version bumps beyond current pins (OpenSSL 3.5.8 and SoftEtherVPN v4.44 are already the latest releases).
- `.kilo/plans/`, generated OpenSSL headers inside the openssl checkout (gitignored/build artifacts).

## Resolution addendum (2026-08-26)
- Push to `hoang-rio/SoftEther-Android-Module` failed with **403** (this machine authenticates as `morteza-taheri`, no write access). Per user decision, the submodule was republished to the user's own copy instead:
  - Pushed `SoftEtherClient` history incl. `0fca2e1` to `https://github.com/morteza-taheri/softether-android-client.git` (`main`).
  - Root `.gitmodules` repointed `SoftEtherClient` → `git@github.com:morteza-taheri/softether-android-client.git`; `git submodule sync` applied; committed as `9e8f46b`.
  - `sstpClient`/`vpnLib` remain on `hoang-rio/*` remotes (public read; pointers already match their remotes).
- Follow-up work in the same session (user-requested): removed the Privacy Policy page from `app/` entirely (root commits `e79b5a2`), rebuilt all four ABI static libraries from the `openssl-3.5.8` tag on a Windows host via MSYS2 (`build-openssl-android.sh` gained Windows-NDK support, commit `f9f8d13`; archives commit `3503c96`), verified via `:app:compileProReleaseKotlin` and `:SoftEtherClient:buildCMakeDebug`. All of it pushed to `morteza-taheri/SoftEther` master (`9e8f46b`).
